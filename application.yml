logging:
  level:
    io:
      spring:
        cloud:
          samples: INFO

fortune:
  fallbackFortune: Your future is very cloudy...

hystrix:
  command:
    default:
      circuitBreaker:
        requestVolumeThreshold: 2
